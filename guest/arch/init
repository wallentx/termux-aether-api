#!/usr/bin/bash
# Minimal PID 1 for the fresh, persistent Arch workspace. No Android paths mounted.
set -eu
export PATH=/usr/local/sbin:/usr/local/bin:/usr/bin
mount -t devtmpfs devtmpfs /dev
mount -t proc proc /proc
mount -t sysfs sysfs /sys
mount -t tmpfs tmpfs /run
mount -t tmpfs tmpfs /tmp
mkdir -p /dev/pts /run/sshd
mount -t devpts devpts /dev/pts -o gid=5,mode=620
ln -sf /proc/self/fd /dev/fd
ln -sf /proc/self/fd/0 /dev/stdin
ln -sf /proc/self/fd/1 /dev/stdout
ln -sf /proc/self/fd/2 /dev/stderr
exec </dev/hvc0 >/dev/hvc0 2>&1
stty -echo
# ext4 replays its journal on mount. Refuse sessions if remounting writable fails.
mount -o remount,rw /
umask 077
mkdir -p /root/.ssh
key=$(dd if=/dev/vdb bs=4096 count=1 status=none | tr -d '\000')
[[ $key =~ ^ssh-ed25519\ [A-Za-z0-9+/]{68}$ ]] || { echo 'Invalid SSH seed'; exit 1; }
printf '%s\n' "$key" > /root/.ssh/authorized_keys
chmod 700 /root /root/.ssh
chmod 600 /root/.ssh/authorized_keys
if [[ ! -e /etc/ssh/ssh_host_ed25519_key ]]; then
    ssh-keygen -q -t ed25519 -N '' -f /etc/ssh/ssh_host_ed25519_key
fi
ip link set lo up
ci=false
for word in $(cat /proc/cmdline); do [[ $word != termux_ci=1 ]] || ci=true; done
if $ci; then
    # QEMU CI-only connectivity. Production config has neither NIC nor this flag.
    ip link set eth0 up
    ip addr add 10.0.2.15/24 dev eth0
    /usr/bin/sshd -D -e -f /etc/ssh/sshd_config.termux -o ListenAddress=0.0.0.0 &
else
    /usr/bin/sshd -D -e -f /etc/ssh/sshd_config.termux &
    /usr/local/sbin/termux-vsock-ssh &
    vsock_pid=$!
    sleep 0.1
    kill -0 "$vsock_pid"
fi
printf '\nTERMUX_ARCH_BOOT_V2\n'
cat /etc/os-release
uname -a
pacman --version
printf 'root_mount: '
findmnt -n -o SOURCE,FSTYPE,OPTIONS /
printf 'TERMUX_ARCH_HOST_KEY_V2 %s\n' "$(cut -d ' ' -f 1,2 /etc/ssh/ssh_host_ed25519_key.pub)"
printf 'TERMUX_ARCH_READY_V2\n'
while true; do
    if IFS= read -r command; then
        case "$command" in
            poweroff)
                kill -TERM -1 || true
                sleep 1
                kill -KILL -1 || true
                sync
                if mount -o remount,ro /; then
                    printf 'TERMUX_ARCH_STOPPING_V2\n'
                    echo o > /proc/sysrq-trigger
                else
                    echo 'TERMUX_ARCH_SHUTDOWN_FAILED: root is still writable'
                fi
                ;;
        esac
    else
        sleep 1
    fi
done
